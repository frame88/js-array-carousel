// const element = document.querySelector('ul');
const elements = document.querySelectorAll('ul');
console.log(elements);
elements[1].classList.add('selected');

console.log(document.getElementsByClassName('element-list'));